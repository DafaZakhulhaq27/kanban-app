import { V1 } from "./versions";

const BASEHOME = '/';
const BASELOGIN = '/login';
const BASEREGISTER = '/register';

const HOME = `${BASEHOME}/${V1}`;
const LOGIN = `${BASELOGIN}/${V1}`;
const REGISTER = `${BASEREGISTER}/${V1}`;

export {
    BASEHOME,
    BASELOGIN,
    BASEREGISTER,
    HOME,
    LOGIN,
    REGISTER
}